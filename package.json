{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@vitejs/plugin-react": "^4.2.1",
    "turbo": "^1.13.3",
    "vite": "^5.2.11"
  },
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build"
  },
  "dependencies": {
    "phosphor-react": "^1.4.1"
  }
}
